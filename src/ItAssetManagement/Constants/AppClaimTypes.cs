namespace ItAssetManagement.Constants;

/// <summary>
/// Claim types beyond the standard ones. Kept here so the string appears once in the
/// codebase rather than being retyped at every read site.
/// </summary>
public static class AppClaimTypes
{
    public const string FullName = "full_name";
}
